/// Seed shape for a fresh editor session. `content` opens with a single
/// text block; `todo` opens with a single empty checklist block and the
/// note's [DisplayMode.withTodoList] hint.
enum NoteType { content, todo }
